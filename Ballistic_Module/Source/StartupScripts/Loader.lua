--!native
--!optimize 2

if not game:IsLoaded() then game.Loaded:Wait() end

local ExploitInfo = {
    ["Name"] = "%EXPLOIT_NAME%",
}

warn(string.format("%s initialized. Please report any bugs you find in our Discord server.", ExploitInfo["Name"]))